{
  "name": "ruralstem-labs",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python vercel_app.py",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run preview"
  }
} 